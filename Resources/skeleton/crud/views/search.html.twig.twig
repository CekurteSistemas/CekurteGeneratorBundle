{% block extends %}
{{ "{% extends 'CekurteGeneratorBundle::modal.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block title -%}{{ 'Pesquisar' | trans }}{% endblock %}" }}

{{ "{% block form_open -%}" }}
    <form class="form-horizontal" role="form" action="{{" {{ path('" ~ route_name_prefix ~ "_search') }}" }}" method="post">
{{ "{% endblock %}" }}

{{ "{% block content -%}" }}
    {{ "{{ form_widget(search_form) }}" }}
{{ "{% endblock %}" }}

{{ "{% block action_close -%}" }}
    <a href="{{" {{ path('" ~ route_name_prefix ~ "_search') }}" }}" class="btn btn-default">{{ "{{ 'Fechar' | trans }}" }}</a>
{{ "{% endblock %}" }}
{% endblock body %}